apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecommerce-app.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "ecommerce-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
