replicaCount: 1

image:
  repository: ghcr.io/mykhailorp/ecommerce-app:latest
  tag: "1.0.0"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "250m"
    memory: "256Mi"

service:
  type: ClusterIP
  port: 80
